#include <stdio.h>
#include <malloc.h>

int main()
{
    float a[4];
    /*a = (float*)malloc(4*sizeof(float));
    if(a != NULL)
    {*/
    scanf("%f", a[0]);
    //}
    //free(a);
    return 0;
}
